global:
  resolve_timeout: 1m

route:
  receiver: "heat-scaler"
  group_by: ["alertname", "server_group"]
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 2m

receivers:
  - name: "heat-scaler"
    webhook_configs:
      - url: "http://localhost:9200"
        send_resolved: false
